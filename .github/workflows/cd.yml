name: CD - RetNova Deployment

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Confirm push event
        run: echo "Push detected. Railway will auto-deploy."
